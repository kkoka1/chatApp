type Message 
@model 
@key (name: "byUserName", fields: ["userName", "createdAt"], queryField: "byUserName")
@auth (rules: [ { allow: public, provider: iam, operations: [create, update, delete, read] } ]){
  id: ID!
  userName: String!
  content: String!
  createdAt: AWSDateTime
}